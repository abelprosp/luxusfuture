# Frontend Next.js

```powershell
npm install
npm run dev
```

Variáveis de ambiente públicas ficam documentadas em `.env.example`.

O painel espera API Go em `http://localhost:8080` (`NEXT_PUBLIC_API_URL`).
