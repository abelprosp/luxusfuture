services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: telefonia
      POSTGRES_PASSWORD: telefonia
      POSTGRES_DB: telefonia
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
